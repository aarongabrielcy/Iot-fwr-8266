# Este archivo puede estar vacío para configuraciones estándar, 
# pero añadir esto fuerza al compilador a mirar en la carpeta del componente.
COMPONENT_ADD_INCLUDEDIRS := include
COMPONENT_DEPENDS := app_core telemetry mqtt_mgr sensors wifi_mgr ota